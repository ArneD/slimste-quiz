<Window x:Class="SlimsteMens.Gui.Admin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="400" MinHeight="400" MinWidth="525" Width="525" Style="{StaticResource WindowStyle}">
    <Window.Resources>
        
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <ContentPresenter Content="{Binding CurrentView}" />
        <Button Grid.Row="1" Height="auto" Click="ButtonBase_OnClick">Wijzig score</Button>
    </Grid>
    
    
</Window>
